slam_toolbox:
  ros__parameters:
    use_sim_time: true

    odom_frame: "odom"
    base_frame: "base_footprint"
    map_frame: "map"

    scan_topic: "/lidar_scan"
    resolution: 0.05
    min_laser_range: 0.1
    max_laser_range: 12.0
    mode: "mapping"
    scan_queue_size: 1
